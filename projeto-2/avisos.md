o `run_all_tests.sh` não vai funcionar porque ainda precisa adaptar o `server.c` e o `client.c`:

1. vou precisar modificar o main do server.c para ler o argumento de **tipo de teste**

2. o cliente não pode ter um fgets para ser rodado em background



